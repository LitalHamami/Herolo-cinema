import React from 'react';
import classes from './Spinner.css';

/**
 * Rerurn a Spinner while loading..
 */
const spinner = () => <div className={classes.loader}>Loading</div>

export default spinner;